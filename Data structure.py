import array as arr

names = arr.array('i', [1, 2, 3])
'''arrays are just like list, and can be more efficient in certain cases '''    # this is a multiline comment
print(names)

names_list = ["alex", "sam", "clover"]

#heaps, stacks and queues
#heaps are either min heap or max heap, which is a sort of tree structure that can return the min or max values quickly
# stacks are based on FILA (first in last out)
# queues are based on FIFO (first in first out)